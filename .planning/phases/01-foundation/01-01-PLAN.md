---
phase: 01-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - server/package.json
  - server/tsconfig.json
  - server/src/app.ts
  - server/src/server.ts
  - server/src/config/env.ts
  - server/src/config/db.ts
  - client/package.json
  - client/tsconfig.json
  - client/index.html
  - client/vite.config.ts
  - .eslintrc.cjs
  - .prettierrc
  - .env.example
  - .gitignore
  - docker-compose.yml
  - Dockerfile
  - .github/workflows/ci.yml
autonomous: true

features:
  implements: ["INFRA-01", "INFRA-02"]
  depends_on: []
  enables: ["INFRA-03", "WINE-01", "WINE-02", "NOTE-01", "NOTE-02", "LIFE-01"]

must_haves:
  truths:
    - "Developer can run `docker compose up` and both API and SPA start without errors"
    - "GitHub Actions CI runs lint, type-check, and build on push to main"
    - "Running `npm run dev` in server/ starts Express on port 3001 with no TypeScript errors"
    - "Running `npm run dev` in client/ starts Vite dev server on port 5173"
    - "Monorepo root has working lint and type-check scripts across both workspaces"
  artifacts:
    - path: "package.json"
      provides: "npm workspaces root with server + client as workspaces"
      contains: "workspaces"
    - path: "server/src/config/env.ts"
      provides: "Zod-validated env config with DATABASE_URL, JWT_SECRET, PORT"
      exports: ["env"]
    - path: "server/src/config/db.ts"
      provides: "Knex instance connected to PostgreSQL"
      exports: ["db"]
    - path: "server/src/app.ts"
      provides: "Express app factory (no side effects on import)"
      exports: ["app"]
    - path: "docker-compose.yml"
      provides: "Local dev stack: postgres + api + client"
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline: lint, type-check, build"
  key_links:
    - from: "server/src/server.ts"
      to: "server/src/app.ts"
      via: "import app, bind port"
      pattern: "app\\.listen"
    - from: "server/src/config/db.ts"
      to: "process.env.DATABASE_URL"
      via: "Knex connection string"
      pattern: "DATABASE_URL"
    - from: "docker-compose.yml"
      to: "server/"
      via: "Docker build context"
      pattern: "build:"
---

<objective>
Bootstrap the monorepo with the exact directory structure from TechArch, configure TypeScript + ESLint + Prettier for both workspaces, set up Docker Compose for local development, and create the GitHub Actions CI pipeline that validates every push to main.

Purpose: Everything in Phases 2–4 depends on this skeleton existing. Without it, no code can be built, tested, or deployed.
Output: A runnable monorepo where `docker compose up` starts the full stack, and CI validates on every push.
</objective>

<feature_dependencies>
Implements: INFRA-01: Monorepo scaffold and tooling, INFRA-02: CI/CD pipeline
Depends on: None
Enables: INFRA-03: Database migrations and auth API; all v1 user requirements (WINE-01, WINE-02, NOTE-01, NOTE-02, LIFE-01) transitively through later phases
</feature_dependencies>

<execution_context>
@/root/.config/opencode/pivota_spec-framework/workflows/execute-plan.md
@/root/.config/opencode/pivota_spec-framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monorepo scaffold — directory structure, packages, TypeScript, ESLint, Prettier, env config</name>
  <files>
    package.json
    server/package.json
    server/tsconfig.json
    server/src/app.ts
    server/src/server.ts
    server/src/config/env.ts
    server/src/config/db.ts
    server/src/middleware/errorHandler.ts
    client/package.json
    client/tsconfig.json
    client/index.html
    client/vite.config.ts
    client/src/main.tsx
    .eslintrc.cjs
    .prettierrc
    .env.example
    .gitignore
  </files>
  <action>
Create the full monorepo scaffold exactly matching TechArch directory structure. This is a greenfield project — no existing files to preserve.

**1. Root package.json** — npm workspaces root:
```json
{
  "name": "wine-inventory",
  "private": true,
  "workspaces": ["server", "client"],
  "scripts": {
    "lint": "npm run lint --workspaces --if-present",
    "type-check": "npm run type-check --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  }
}
```

**2. server/package.json** — Express + TypeScript backend:
```json
{
  "name": "wine-inventory-server",
  "version": "0.1.0",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js",
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit",
    "test": "jest --passWithNoTests",
    "migrate": "knex migrate:latest",
    "migrate:rollback": "knex migrate:rollback"
  },
  "dependencies": {
    "express": "^4.18.2",
    "knex": "^3.1.0",
    "pg": "^8.11.3",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "zod": "^3.22.4",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5",
    "helmet": "^7.1.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/bcryptjs": "^2.4.6",
    "@types/jsonwebtoken": "^9.0.5",
    "@types/cors": "^2.8.17",
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "^6.14.0",
    "@typescript-eslint/parser": "^6.14.0",
    "eslint": "^8.55.0",
    "jest": "^29.7.0",
    "@types/jest": "^29.5.11",
    "ts-jest": "^29.1.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
```

**3. server/tsconfig.json**:
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "module": "commonjs",
    "lib": ["ES2020"],
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

**4. server/src/config/env.ts** — Zod-validated environment:
```typescript
import { z } from 'zod';
import dotenv from 'dotenv';

dotenv.config();

const envSchema = z.object({
  NODE_ENV: z.enum(['development', 'test', 'production']).default('development'),
  PORT: z.string().default('3001').transform(Number),
  DATABASE_URL: z.string().url(),
  JWT_SECRET: z.string().min(32),
  JWT_REFRESH_SECRET: z.string().min(32),
  JWT_ACCESS_EXPIRES_IN: z.string().default('1h'),
  JWT_REFRESH_EXPIRES_IN: z.string().default('30d'),
  CORS_ORIGIN: z.string().default('http://localhost:5173'),
});

export const env = envSchema.parse(process.env);
export type Env = z.infer<typeof envSchema>;
```

**5. server/src/config/db.ts** — Knex instance:
```typescript
import knex from 'knex';
import { env } from './env';

export const db = knex({
  client: 'pg',
  connection: env.DATABASE_URL,
  pool: { min: 2, max: 10 },
  migrations: {
    directory: './migrations',
    extension: 'ts',
  },
});
```

**6. server/src/app.ts** — Express app factory (no `app.listen` here):
```typescript
import express from 'express';
import cors from 'cors';
import helmet from 'helmet';
import { env } from './config/env';
import { errorHandler } from './middleware/errorHandler';

export function createApp() {
  const app = express();

  app.use(helmet());
  app.use(cors({ origin: env.CORS_ORIGIN, credentials: true }));
  app.use(express.json());

  app.get('/health', (_req, res) => {
    res.json({ status: 'ok', timestamp: new Date().toISOString() });
  });

  // Routes registered here in later plans
  // app.use('/api/v1/auth', authRouter);
  // app.use('/api/v1/wines', winesRouter);

  app.use(errorHandler);

  return app;
}

export const app = createApp();
```

**7. server/src/server.ts** — Entry point:
```typescript
import { app } from './app';
import { env } from './config/env';
import { db } from './config/db';

async function main() {
  // Verify DB connection
  await db.raw('SELECT 1');
  console.log('Database connection verified');

  app.listen(env.PORT, () => {
    console.log(`Server running on port ${env.PORT} in ${env.NODE_ENV} mode`);
  });
}

main().catch((err) => {
  console.error('Failed to start server:', err);
  process.exit(1);
});
```

**8. server/src/middleware/errorHandler.ts**:
```typescript
import { Request, Response, NextFunction } from 'express';

export interface AppError extends Error {
  statusCode?: number;
  code?: string;
}

export function errorHandler(
  err: AppError,
  _req: Request,
  res: Response,
  _next: NextFunction
): void {
  const statusCode = err.statusCode ?? 500;
  const code = err.code ?? 'INTERNAL_ERROR';

  if (process.env.NODE_ENV !== 'test') {
    console.error(`[${code}] ${err.message}`, err.stack);
  }

  res.status(statusCode).json({
    error: { code, message: err.message },
  });
}
```

**9. client/package.json** — Vite + React + TypeScript frontend:
```json
{
  "name": "wine-inventory-client",
  "version": "0.1.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint src --ext .ts,.tsx",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.20.1",
    "@tanstack/react-query": "^5.13.4",
    "zustand": "^4.4.7",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "@types/react": "^18.2.43",
    "@types/react-dom": "^18.2.17",
    "@typescript-eslint/eslint-plugin": "^6.14.0",
    "@typescript-eslint/parser": "^6.14.0",
    "@vitejs/plugin-react": "^4.2.1",
    "eslint": "^8.55.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "eslint-plugin-react-refresh": "^0.4.5",
    "typescript": "^5.3.3",
    "vite": "^5.0.8",
    "vitest": "^1.0.4"
  }
}
```

**10. client/tsconfig.json**:
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

Also create client/tsconfig.node.json:
```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true
  },
  "include": ["vite.config.ts"]
}
```

**11. client/vite.config.ts**:
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:3001',
        changeOrigin: true,
      },
    },
  },
});
```

**12. client/index.html** — Vite entry point:
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Wine Inventory</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**13. client/src/main.tsx** — React entry point stub (routes added in plan 01-03):
```tsx
import React from 'react';
import ReactDOM from 'react-dom/client';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <div>Wine Inventory — loading...</div>
  </React.StrictMode>
);
```

**14. .eslintrc.cjs** — Root ESLint config (applies to both workspaces):
```javascript
module.exports = {
  root: true,
  env: { es2020: true, node: true },
  extends: [
    'eslint:recommended',
    'plugin:@typescript-eslint/recommended',
  ],
  parser: '@typescript-eslint/parser',
  parserOptions: { ecmaVersion: 'latest', sourceType: 'module' },
  plugins: ['@typescript-eslint'],
  rules: {
    '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_' }],
    '@typescript-eslint/explicit-function-return-type': 'off',
    '@typescript-eslint/no-explicit-any': 'warn',
  },
};
```

**15. .prettierrc**:
```json
{
  "semi": true,
  "singleQuote": true,
  "trailingComma": "es5",
  "printWidth": 100,
  "tabWidth": 2
}
```

**16. .env.example** — Template for required environment variables:
```
# Server
NODE_ENV=development
PORT=3001
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/wine_inventory
JWT_SECRET=change-me-at-least-32-characters-long
JWT_REFRESH_SECRET=change-me-refresh-at-least-32-chars
JWT_ACCESS_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=30d
CORS_ORIGIN=http://localhost:5173

# Client (Vite prefix required)
VITE_API_URL=http://localhost:3001
```

**17. .gitignore**:
```
node_modules/
dist/
.env
*.local
.DS_Store
*.tsbuildinfo
coverage/
```

Also create the full directory structure from TechArch (empty placeholder directories with .gitkeep as needed):
- server/migrations/
- server/tests/unit/
- server/tests/integration/
- server/src/routes/ (create .gitkeep)
- server/src/controllers/ (create .gitkeep)
- server/src/services/ (create .gitkeep)
- server/src/repositories/ (create .gitkeep)
- server/src/types/ (create .gitkeep)
- server/src/middleware/ (already has errorHandler.ts)
- client/src/api/
- client/src/pages/
- client/src/components/
- client/src/hooks/
- client/src/store/
- client/src/utils/

After creating all files, install dependencies:
```bash
cd /path/to/project && npm install
```
  </action>
  <verify>
```bash
# Verify both workspace packages install cleanly
npm install 2>&1 | tail -5

# Verify TypeScript compiles without errors
cd server && npm run type-check 2>&1 | tail -10 && echo "SERVER TYPE-CHECK OK"
cd ../client && npm run type-check 2>&1 | tail -10 && echo "CLIENT TYPE-CHECK OK"

# Verify env.ts loads (will fail if schema invalid — expected without real .env)
ls server/src/config/env.ts server/src/config/db.ts server/src/app.ts server/src/server.ts && echo "CORE SERVER FILES EXIST"

# Verify directory structure matches TechArch
ls server/src/routes server/src/controllers server/src/services server/src/repositories server/src/types server/migrations && echo "SERVER DIRS OK"
ls client/src/api client/src/pages client/src/components client/src/hooks client/src/store client/src/utils && echo "CLIENT DIRS OK"
```
  </verify>
  <done>
- `npm install` completes with no errors at the root
- `server: npm run type-check` exits 0 (no TypeScript errors)
- `client: npm run type-check` exits 0 (no TypeScript errors)
- All directories from TechArch exist in server/ and client/
- server/src/config/env.ts, db.ts, app.ts, server.ts all exist with exact content above
- .env.example documents all required environment variables
  </done>
</task>

<task type="auto">
  <name>Task 2: Docker Compose local dev stack and GitHub Actions CI pipeline</name>
  <files>
    docker-compose.yml
    Dockerfile
    .github/workflows/ci.yml
  </files>
  <action>
**1. docker-compose.yml** — Local development stack (postgres + api + client):
```yaml
version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: wine_inventory
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres']
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build:
      context: ./server
      dockerfile: ../Dockerfile
      target: dev
    environment:
      NODE_ENV: development
      PORT: 3001
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/wine_inventory
      JWT_SECRET: local-dev-secret-at-least-32-characters
      JWT_REFRESH_SECRET: local-refresh-secret-at-least-32-chars
      JWT_ACCESS_EXPIRES_IN: 1h
      JWT_REFRESH_EXPIRES_IN: 30d
      CORS_ORIGIN: http://localhost:5173
    ports:
      - '3001:3001'
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./server:/app
      - /app/node_modules
    command: npm run dev

  client:
    image: node:20-alpine
    working_dir: /app
    environment:
      VITE_API_URL: http://localhost:3001
    ports:
      - '5173:5173'
    depends_on:
      - api
    volumes:
      - ./client:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev -- --host"

volumes:
  postgres_data:
```

**2. Dockerfile** — Multi-stage for dev and production:
```dockerfile
# Development stage
FROM node:20-alpine AS dev
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
EXPOSE 3001
CMD ["npm", "run", "dev"]

# Build stage
FROM node:20-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
RUN npm run build

# Production stage
FROM node:20-alpine AS production
WORKDIR /app
COPY --from=build /app/dist ./dist
COPY --from=build /app/node_modules ./node_modules
COPY package*.json ./
EXPOSE 3001
USER node
CMD ["node", "dist/server.js"]
```

Place Dockerfile at project root (it uses `./server` as build context from docker-compose). Actually, place a dedicated Dockerfile inside `server/` so docker-compose build context is `./server`:

Create `server/Dockerfile` with the content above (the root `Dockerfile` is for CI/CD purposes).

Update docker-compose.yml build context:
```yaml
  api:
    build:
      context: ./server
      dockerfile: Dockerfile
      target: dev
```

**3. .github/workflows/ci.yml** — CI pipeline that runs on every push and PR to main:
```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-typecheck:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - name: Lint server
        run: npm run lint -w server
      - name: Type-check server
        run: npm run type-check -w server
      - name: Lint client
        run: npm run lint -w client
      - name: Type-check client
        run: npm run type-check -w client

  test:
    name: Tests
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: wine_inventory_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      NODE_ENV: test
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/wine_inventory_test
      JWT_SECRET: test-secret-at-least-32-characters-long
      JWT_REFRESH_SECRET: test-refresh-secret-at-least-32-chars
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - name: Run server tests
        run: npm run test -w server
      - name: Run client tests
        run: npm run test -w client

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [lint-and-typecheck]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci
      - name: Build server
        run: npm run build -w server
        env:
          DATABASE_URL: postgresql://placeholder:placeholder@localhost:5432/placeholder
          JWT_SECRET: build-placeholder-secret-32-characters
          JWT_REFRESH_SECRET: build-placeholder-refresh-32-chars
      - name: Build client
        run: npm run build -w client
        env:
          VITE_API_URL: https://placeholder.render.com
```

Create `.github/workflows/` directory if it does not exist.
  </action>
  <verify>
```bash
# Validate docker-compose config
docker compose config --quiet && echo "COMPOSE CONFIG VALID"

# Verify Dockerfile exists in server/
ls server/Dockerfile && echo "SERVER DOCKERFILE EXISTS"

# Verify CI workflow exists
ls .github/workflows/ci.yml && echo "CI WORKFLOW EXISTS"

# Start the full stack (requires .env or docker-compose env vars)
docker compose up -d && sleep 15 && docker compose ps && echo "STACK STARTED"

# Verify API health endpoint responds
curl -sf http://localhost:3001/health && echo "API HEALTH OK" || echo "API not yet reachable (may need migrations)"

# Tear down
docker compose down && echo "STACK STOPPED"
```
  </verify>
  <done>
- `docker compose config --quiet` exits 0 (valid YAML)
- `server/Dockerfile` exists with dev, build, and production stages
- `.github/workflows/ci.yml` exists and defines lint-and-typecheck, test, and build jobs
- `docker compose up -d` starts postgres, api, and client services — all reach `Up` state
- `docker compose ps` shows 3 running services (postgres, api, client)
- `docker compose down` stops all containers cleanly
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
```bash
# 1. Full monorepo install
npm install && echo "INSTALL OK"

# 2. TypeScript clean across both workspaces
npm run type-check -w server && npm run type-check -w client && echo "TYPE-CHECK PASSED"

# 3. Directory structure matches TechArch exactly
ls server/src/config server/src/middleware server/src/routes server/src/controllers \
   server/src/services server/src/repositories server/src/types server/migrations \
   server/tests/unit server/tests/integration && echo "SERVER STRUCTURE OK"
ls client/src/api client/src/pages client/src/components client/src/hooks \
   client/src/store client/src/utils && echo "CLIENT STRUCTURE OK"

# 4. Docker Compose spins up
docker compose up -d && sleep 15 && docker compose ps

# 5. Health endpoint (no auth required)
curl -sf http://localhost:3001/health | grep '"status":"ok"' && echo "HEALTH OK"

docker compose down
```
</verification>

<success_criteria>
1. `npm install` succeeds at monorepo root — both workspaces installed
2. `npm run type-check` passes for server (0 TypeScript errors)
3. `npm run type-check` passes for client (0 TypeScript errors)
4. All TechArch directories exist in server/src/ and client/src/
5. `docker compose up -d` brings all 3 services to `Up` state
6. `GET /health` returns `{"status":"ok"}` from the API container
7. `.github/workflows/ci.yml` defines lint, type-check, test, and build jobs
8. `.env.example` documents all required environment variables
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` documenting:
- Exact package versions installed (from package-lock.json)
- Any deviations from the TechArch directory structure
- Docker Compose service names and ports
- Environment variables required to start the stack
- CI workflow trigger conditions and job names
</output>
